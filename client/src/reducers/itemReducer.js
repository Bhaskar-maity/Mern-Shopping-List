import uuid from 'uuid';

const initialState = {
    items: [
        { id: uuid(), name: 'a'},
        { id: uuid(), name: 'b'},
        { id: uuid(), name: 'c'},
        { id: uuid(), name: 'd'}
    ]
}